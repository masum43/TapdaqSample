dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        jcenter()

        //Tapdaq SDK and Adapters
        maven { url "https://android-sdk.tapdaq.com" }
    }
}
rootProject.name = "TapdaqSample"
include ':app'
